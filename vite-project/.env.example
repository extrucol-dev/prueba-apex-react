# Copia este archivo a `.env` y ajusta segun tu instancia ORDS.
# El proxy de Vite reescribe /api -> VITE_ORDS_BASE en dev (ver vite.config.js).

VITE_ORDS_BASE=http://localhost:8080/ords/ventas

# Cuando este flag es "true" la app usa datos mock y NO llama a ORDS.
VITE_USE_MOCKS=false
